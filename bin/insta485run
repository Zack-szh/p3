#!/bin/bash
set -Eeuo pipefail

pkill -f "flask --app insta485" || true
if [ ! -f "var/insta485.sqlite3" ]; then 
    echo "Error: can't find database var/insta485.sqlite3
Try: ./bin/insta485db create"
    exit 1
else
    flask --app insta485 --debug run --host 0.0.0.0 --port 8000
fi 


